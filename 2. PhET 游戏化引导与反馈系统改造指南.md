---
title: PhET 游戏化引导与反馈系统改造指南
stage: "第二步：游戏化引导与反馈系统改造"
stage_index: 2
prev_stage: "第一步：模拟器本地复现"
prev_document: "../PHET-SIMULATION-REPRODUCTION-WORKFLOW.md"
version: "v1.2"
last_updated: "2026-03-04"
projects:
  - forces-and-motion-basics  # 首个参考实现项目
  - energy-skate-park         # 第二个移植项目，4屏复杂结构
---
# PhET 游戏化引导与反馈系统改造指南

本文档提供了将 PhET 模拟器改造为具有新手引导和正反馈系统的通用指南和模板。本指南基于 `forces-and-motion-basics` 项目的成功改造经验。

---

## 目录

1. [改造概述](#改造概述)
2. [系统架构](#系统架构)
3. [实施步骤](#实施步骤)
4. [核心组件](#核心组件)
5. [配置指南](#配置指南)
6. [最佳实践](#最佳实践)
7. [常见问题](#常见问题)
8. [风险提示](#风险提示)
9. [快速开始检查清单](#快速开始检查清单)
10. [检查清单](#检查清单)
11. [附录](#附录)
12. [Energy Skate Park 移植案例与经验沉淀](#energy-skate-park-移植案例与经验沉淀)

---

## 改造概述

### 目标

为 PhET 模拟器添加以下功能：

1. **新手引导系统**: 分步引导首次用户了解核心功能
2. **正反馈系统**: 基于用户操作提供即时鼓励和教育反馈
3. **中文本地化**: 完整的中文界面支持
4. **UI 美化**: 提升视觉体验和交互流畅度

### 适用范围

本指南适用于具有以下特征的 PhET 模拟器：

- 多个游戏/实验屏幕
- 可操作的控件（滑块、按钮、拖拽对象）
- 可观察的物理现象或数据变化
- 需要用户理解特定概念

---

## 系统架构

### 组件层次图

```
┌─────────────────────────────────────────────────────────────┐
│                     ScreenView (屏幕视图)                      │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐                 │
│  │ TutorialOverlay  │  │ FeedbackManager  │                 │
│  │   (引导覆盖层)    │  │   (反馈管理器)    │                 │
│  └──────────────────┘  └────────┬─────────┘                 │
│                                │                            │
│                       ┌────────▼─────────┐                  │
│                       │ FeedbackToastNode │                  │
│                       │   (反馈提示框)    │                  │
│                       └──────────────────┘                  │
│                                                              │
│  ┌──────────────────────────────────────────────────┐       │
│  │              Feedback Rules (反馈规则)            │       │
│  │  • ScreenSpecificRules (屏幕特定规则)             │       │
│  │  • CommonRules (通用规则)                        │       │
│  └──────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 数据流

```
用户操作 → 模型状态变化 → FeedbackManager.update()
                              ↓
                         规则评估 (按优先级)
                              ↓
                         规则匹配检查
                              ↓
                         冷却机制检查
                              ↓
                      FeedbackToastNode.show()
```

---

## 实施步骤

### 步骤 1: 创建公共组件目录结构

```bash
cd your-sim/js/common

# 创建视图组件目录
mkdir -p view
mkdir -p model
```

### 步骤 2: 复制核心组件

从 `forces-and-motion-basics` 复制以下文件到你的项目：

```
forces-and-motion-basics/js/common/view/TutorialOverlayNode.ts
  → your-sim/js/common/view/TutorialOverlayNode.ts

forces-and-motion-basics/js/common/view/FeedbackToastNode.ts
  → your-sim/js/common/view/FeedbackToastNode.ts

forces-and-motion-basics/js/common/view/WelcomeDialog.ts
  → your-sim/js/common/view/WelcomeDialog.ts

forces-and-motion-basics/js/common/view/GameCardTooltipNode.ts
  → your-sim/js/common/view/GameCardTooltipNode.ts

forces-and-motion-basics/js/common/model/FeedbackManager.ts
  → your-sim/js/common/model/FeedbackManager.ts
```

### 步骤 3: 修改命名空间注册

在每个复制的文件中，修改以下内容：

```typescript
// 原始
import forcesAndMotionBasics from '../../forcesAndMotionBasics.js';
forcesAndMotionBasics.register('TutorialOverlayNode', TutorialOverlayNode);

// 修改为
import yourSimName from '../../yourSimName.js';
yourSimName.register('TutorialOverlayNode', TutorialOverlayNode);
```

### 步骤 4: 创建反馈规则文件

为每个屏幕创建反馈规则文件：

```typescript
// your-sim/js/yourScreen/view/YourScreenFeedbackRules.ts

import type YourModel from '../../model/YourModel.js';
import { FeedbackRule } from '../../../common/model/FeedbackManager.js';

export const yourScreenFeedbackRules: FeedbackRule[] = [
  {
    id: 'rule-unique-id',
    check: (model: YourModel) => {
      // 返回 true 当规则应该触发
      return model.someProperty.value > threshold;
    },
    message: '反馈消息文本',
    type: 'discovery', // 'success' | 'discovery' | 'encouragement'
    priority: 1        // 数字越大优先级越高
  }
  // 添加更多规则...
];
```

### 步骤 5: 修改屏幕视图

在每个 ScreenView 中进行以下修改：

#### 5.1 添加导入

```typescript
import FeedbackManager from '../../common/model/FeedbackManager.js';
import { yourScreenFeedbackRules } from './YourScreenFeedbackRules.js';
import TutorialOverlayNode, { type TutorialStep } from '../../common/view/TutorialOverlayNode.js';
```

#### 5.2 添加私有属性

```typescript
export default class YourScreenView extends ScreenView {
  private readonly feedbackManager: FeedbackManager;
  private readonly tutorialOverlay: TutorialOverlayNode;

  // 将需要在引导中引用的控件改为实例属性
  private readonly mainControl: YourControlNode;
  // ...
}
```

#### 5.3 初始化反馈系统

```typescript
public constructor(model: YourModel, tandem: Tandem) {
  super(model, tandem);

  // ... 原有代码 ...

  // 初始化 FeedbackManager
  this.feedbackManager = new FeedbackManager({
    tandem: tandem.createTandem('feedbackManager'),
    layoutBounds: this.layoutBounds
  });

  // 注册反馈规则
  yourScreenFeedbackRules.forEach(rule => {
    this.feedbackManager.registerRule(rule);
  });

  // 设置父节点
  this.feedbackManager.setParentNode(this);

  // 链接模型更新
  model.stepEmitter.addListener(() => {
    this.feedbackManager.update(model);
  });

  // 重置时清空反馈
  model.resetAllEmitter.addListener(() => {
    this.feedbackManager.reset();
  });

  // 初始化 Tutorial
  this.tutorialOverlay = new TutorialOverlayNode({
    layoutBounds: this.layoutBounds,
    steps: this.getTutorialSteps(),
    onComplete: () => {
      this.feedbackManager.setEnabled(true);
      setTimeout(() => {
        this.feedbackManager.showFirstFeedback(model);
      }, 400);
    }
  });
  this.addChild(this.tutorialOverlay);

  // 延迟显示引导
  setTimeout(() => {
    this.feedbackManager.setEnabled(false);
    this.tutorialOverlay.restart();
  }, 1500);
}
```

#### 5.6 添加引导步骤方法

```typescript
private getTutorialSteps(): TutorialStep[] {
  return [
    {
      target: this.mainControl,
      title: '引导标题',
      content: '引导内容描述',
      position: 'right', // 'top' | 'bottom' | 'left' | 'right' | 'center'
      onShow: () => {
        // 可选：显示时执行的回调
      },
      onHide: () => {
        // 可选：隐藏时执行的回调
      }
    }
    // 添加更多步骤...
  ];
}
```

### 步骤 6: 更新字符串文件

在 `your-sim-strings_en.yaml` 中添加：

```yaml
# Tutorial (新增：新手引导)
tutorial:
  next: 下一步
  skip: 跳过
  gotIt: 知道了！
  yourScreen:
    step1:
      title: 第一步标题
      content: 第一步内容描述
    # 添加更多步骤...

# Feedback Messages (新增：正反馈消息)
feedback:
  yourScreen:
    ruleId: 反馈消息文本
    # 添加更多消息...
```

### 步骤 7: 运行构建命令

```bash
# 更新字符串子系统
grunt modulify --targets=strings

# 类型检查
grunt type-check

# 代码检查
grunt lint --fix

# 构建项目
npm run build
```

---

## 核心组件

### TutorialOverlayNode (引导覆盖层)

#### 配置选项

```typescript
interface TutorialOverlayNodeOptions {
  steps?: TutorialStep[];           // 引导步骤数组
  onComplete?: () => void;          // 完成回调
  layoutBounds?: Bounds2;           // 布局边界
}
```

#### TutorialStep 接口

```typescript
interface TutorialStep {
  /**
   * 目标节点、获取函数、或 null（全屏遮罩无镂空）。
   * ⚠️ 移植经验：null 表示全屏模式，隐藏高亮框，内容居中显示。
   *    适用于开场欢迎步骤。不要把 null 误用于无法获取的目标节点——
   *    那样做应该换成能稳定获取的节点，而非 null。
   */
  target: Node | (() => Node | null) | null;
  title: string;                        // 步骤标题
  content: string;                      // 步骤内容（支持 HTML）
  position?: 'top' | 'bottom' | 'left' | 'right' | 'center';
  onShow?: () => void;  // 显示时回调，可用于激活引导目标所需的 UI 状态
  onHide?: () => void;  // 隐藏时回调
}
```

> **移植经验** — `target: null` 的使用时机：  
> 每屏第一步建议使用 `null`（全屏暗化 + 居中弹窗），起到「欢迎」作用，不框选任何控件。后续步骤必须指定真实存在的节点。

#### 方法

```typescript
// 重启引导
public restart(): void

// 标记为已查看（存入 localStorage）
public markAsSeen(storageKey: string): void

// 动态更新步骤
public setSteps(steps: TutorialStep[]): void
```

### FeedbackManager (反馈管理器)

#### 配置选项

```typescript
interface FeedbackManagerOptions {
  tandem: Tandem;
  layoutBounds: Bounds2;
}
```

#### FeedbackRule 接口

```typescript
interface FeedbackRule {
  id: string;                                    // 唯一标识符
  check: (model: any) => boolean;                // 触发条件函数
  message: string | ((model: any) => string);    // 静态或动态消息
  type: 'success' | 'discovery' | 'encouragement';
  priority: number;                              // 优先级（越大越高）
}
```

#### 方法

```typescript
// 注册规则
public registerRule(rule: FeedbackRule): void

// 注销规则
public unregisterRule(ruleId: string): void

// 更新反馈（每帧调用）
public update(model: any): void

// 重置状态
public reset(): void

// 重置单个规则
public resetRule(ruleId: string): void

// 启用/禁用
public setEnabled(enabled: boolean): void

// 立即显示第一条反馈
public showFirstFeedback(model?: any): void
```

#### 冷却机制

- **规则级别**: 每个规则只触发一次（通过 `triggeredRules` Set 追踪）
- **全局冷却**: 默认 3 秒间隔（`GLOBAL_COOLDOWN`）
- **类型冷却**:
  - `success`: 5 秒
  - `discovery`: 4 秒
  - `encouragement`: 8 秒

### FeedbackToastNode (反馈提示框)

#### 配置选项

```typescript
interface FeedbackToastNodeOptions {
  layoutBounds: Bounds2;
  message?: string;
  type?: FeedbackType;
  autoHideDelay?: number;    // 默认 4500ms
  position?: 'top' | 'bottom'; // 默认 'top'
}
```

#### 方法

```typescript
// 显示反馈
public show(message: string, type: FeedbackType): void

// 隐藏反馈
public hide(): void

// 强制隐藏（无动画）
public forceHide(): void

// 设置位置
public setPosition(position: 'top' | 'bottom'): void
```

### WelcomeDialog (欢迎对话框)

欢迎对话框在用户首次打开模拟器时显示，提供友好的欢迎界面。

#### 配置选项

```typescript
interface WelcomeDialogOptions {
  onStart?: () => void;  // 开始探索按钮的回调
}
```

#### 方法

```typescript
// 继承自 PopupDialog
// 显示/隐藏由父类控制
```

#### 样式配置

```typescript
// 尺寸
const contentWidth = 500;
const contentHeight = 350;
const cornerRadius = 16;

// 颜色
const BACKGROUND_GRADIENT = {
  start: '#1a1a2e',
  end: '#16213e'
};
const ACCENT_COLOR = '#00f2ff';
const TEXT_COLOR = '#cbd5e1';

// 字体
titleFont: PhetFont({ size: 28, weight: 'bold' });
messageFont: PhetFont(16);
buttonFont: PhetFont({ size: 18, weight: 'bold' });
```

#### 集成方式

在 `HomeScreenView.ts` 中：

```typescript
import WelcomeDialog from '../../common/view/WelcomeDialog.js';

private showWelcomeDialog(): void {
  // 检查是否已显示过
  const hasShownBefore = sessionStorage.getItem( 'phet-welcome-dialog-closed' );
  if ( hasShownBefore ) {
    return;
  }

  // 创建欢迎对话框
  const welcomeDialog = new WelcomeDialog( {
    onStart: () => {
      // 关闭对话框并标记
      welcomeDialog.hide();
      sessionStorage.setItem( 'phet-welcome-dialog-closed', 'true' );
    }
  } );

  this.addChild( welcomeDialog );
  welcomeDialog.show();
}
```

### GameCardTooltipNode (游戏卡片提示)

游戏卡片提示在用户鼠标悬停在首页游戏卡片上时显示，提供游戏的简要介绍。

#### 配置选项

```typescript
interface GameCardTooltipNodeOptions {
  title?: string;           // 标题文本
  description?: string;     // 描述文本（支持 HTML）
  targetNode?: Node | null; // 目标节点
  visible?: boolean;        // 初始可见性
  opacity?: number;         // 初始透明度
}
```

#### 方法

```typescript
// 更新提示内容
public setContent(title: string, description: string): void

// 相对于目标节点定位
public positionRelativeTo(targetNode: Node, layoutBounds: Bounds2): void

// 显示提示（带淡入动画）
public show(): void

// 隐藏提示（带淡出动画）
public hide(): void
```

#### 样式配置

```typescript
// 尺寸
const DEFAULT_WIDTH = 280;
const DEFAULT_HEIGHT = 120;
const CORNER_RADIUS = 8;
const MARGIN = 15;

// 颜色
const BACKGROUND_COLOR = 'rgba(26, 26, 46, 0.95)';
const BORDER_COLOR = 'rgba(0, 242, 255, 0.3)';
const TITLE_COLOR = '#00f2ff';
const TEXT_COLOR = '#cbd5e1';

// 字体
titleFont: PhetFont({ size: 16, weight: 'bold' });
descriptionFont: PhetFont(13);
```

#### 悬浮事件集成

在 `HomeScreenButton.ts` 或类似组件中：

```typescript
import GameCardTooltipNode from '../../common/view/GameCardTooltipNode.js';

// 创建提示节点
private readonly tooltipNode: GameCardTooltipNode = new GameCardTooltipNode({
  title: '游戏标题',
  description: '游戏描述文本',
  visible: false,
  opacity: 0
});

this.addChild( this.tooltipNode );

// 添加悬浮监听
this.addInputListener( {
  enter: () => {
    this.tooltipNode.positionRelativeTo( this, this.layoutBounds );
    this.tooltipNode.show();
  },
  exit: () => {
    this.tooltipNode.hide();
  }
} );
```

---

## 配置指南

### 反馈规则编写指南

#### 规则设计原则

1. **明确触发条件**: 条件应该清晰且容易理解
2. **提供教育价值**: 消息应该帮助用户理解概念
3. **避免重复**: 每个规则只触发一次
4. **合理优先级**: 重要的发现应该优先显示
5. **保持简洁**: 消息长度适中，易于快速阅读

#### 规则类型使用场景

| 类型 | 使用场景 | 示例 |
|------|----------|------|
| success | 完成目标、重大发现 | 游戏获胜、发现物理定律 |
| discovery | 理解概念、观察现象 | 观察到加速度变化 |
| encouragement | 用户卡顿、长时间无进展 | 鼓励尝试不同操作 |

#### 规则编写模板

```typescript
// 简单规则 - 静态消息
{
  id: 'simple-rule',
  check: (model) => model.property.value > 100,
  message: '数值超过100了！',
  type: 'discovery',
  priority: 1
}

// 动态消息规则
{
  id: 'dynamic-rule',
  check: (model) => model.property.value > 100,
  message: (model) => `当前数值是 ${model.property.value.toFixed(1)}`,
  type: 'discovery',
  priority: 1
}

// 复杂条件规则
{
  id: 'complex-rule',
  check: (model) => {
    const condition1 = model.prop1.value > threshold;
    const condition2 = model.prop2.value < limit;
    const condition3 = model.flagProperty.value === true;
    return condition1 && condition2 && condition3;
  },
  message: '多个条件同时满足了！',
  type: 'success',
  priority: 10
}
```

### 引导步骤编写指南

#### 步骤设计原则

1. **循序渐进**: 从简单到复杂
2. **突出重点**: 每步只介绍一个核心功能
3. **可操作**: 确保用户可以立即尝试
4. **适时显示**: 在合适的时机显示相关步骤
5. **保持简洁**: 每步内容不超过3行
6. **避免多语言硬编码 (架构视角)**: 如果是要复用的 `Common` 库组件，组件内的文字（如“下一步”）应从 `YourSimStrings` 中动态读取而不要硬编码。

#### 步骤编写模板

```typescript
{
  target: this.controlNode,  // 直接引用节点
  title: '功能介绍',
  content: '这是功能的详细说明',
  position: 'right'
}

// 动态目标（需要时才获取）
{
  target: () => this.findActiveNode(),
  title: '动态目标',
  content: '目标可能在运行时才确定',
  position: 'bottom'
}

// 带回调的步骤
{
  target: this.displayOption,
  title: '显示数据',
  content: '勾选此选项显示数值',
  position: 'left',
  onShow: () => {
    this.model.showValuesProperty.value = true;
  }
  // onHide 不定义，保持显示状态
}
```

#### 位置选择建议

| 位置 | 适用场景 |
|------|----------|
| right | 控件在左侧，有足够右侧空间 |
| left | 控件在右侧，有足够左侧空间 |
| top | 控件在下方，有足够上方空间 |
| bottom | 控件在上方，有足够下方空间 |
| center | 全屏提示或无明确目标 |

---

## 最佳实践

### 0. 遮罩实现方案选择（重要）

> **移植经验（Energy Skate Park）** — 切勿自行实现四面板切割遮罩方案。

#### ❌ 四面板切割方案（已废弃）

曾尝试用四个 `Rectangle` 面板（top/bottom/left/right）拼接出「镂空」效果：

```typescript
// ❌ 不要这样做 — 坐标计算复杂且容易出错
this.topPanel.setRectBounds(new Bounds2(0, 0, lw, hy));
this.bottomPanel.setRectBounds(new Bounds2(0, hy2, lw, lh));
this.leftPanel.setRectBounds(new Bounds2(0, hy, hx, hy2));
this.rightPanel.setRectBounds(new Bounds2(hx2, hy, lw, hy2));
```

**问题**：坐标计算复杂，一旦 `targetLocalBounds` 有误，四块面板会留出缺口或错位，遮罩只覆盖一半屏幕。

#### ✅ 单层全屏 backdrop + 高亮边框（推荐）

与 `forces-and-motion-basics` 保持一致的方案：

```typescript
// ✅ 正确方案 — 简单可靠
this.backdrop = new Rectangle(0, 0, this.layoutBounds.width, this.layoutBounds.height, {
    fill: 'rgba(0, 0, 0, 0.72)',
    pickable: true
});
this.addChild(this.backdrop);

// 高亮框叠加在 backdrop 上（透明填充，仅显示边框）
this.highlightBox = new Rectangle(0, 0, 100, 100, {
    stroke: '#f0a500',
    lineWidth: 3,
    cornerRadius: 8,
    fill: 'rgba(0, 0, 0, 0)', // 透明内部
    pickable: false
});
```

这样 backdrop 天然覆盖全屏，高亮框只是装饰性边框，不需要复杂的坐标切割。

---

### 0.1 坐标转换关键规则（重要）

> **移植经验（Energy Skate Park）** — 坐标转换是最容易出错的地方。

PhET `ScreenView` 有 **scale + translate transform**（将 1024×618 逻辑坐标适配到物理像素）。TutorialOverlayNode 作为 ScreenView 的子节点，其本地坐标系就是 ScreenView 的逻辑坐标系（1024×618）。

#### ❌ 错误用法（高亮框只覆盖一半）

```typescript
// ❌ 错误：用 this.globalToLocalPoint 转换
const tl = this.globalToLocalPoint(new Vector2(gb.minX, gb.minY));
const br = this.globalToLocalPoint(new Vector2(gb.maxX, gb.maxY));
```

`this.globalToLocalPoint` 在 TutorialOverlayNode 上调用，等效于 ScreenView 坐标系，看似正确，但在某些 PhET 版本中，`this` 和 `this.parent` 的变换矩阵可能不一致（尤其是 ScreenView 内部有额外的缩放层），导致结果偏差。

#### ✅ 正确用法（与 forces-and-motion-basics 保持一致）

```typescript
// ✅ 正确：始终通过 parent（ScreenView）来作坐标反算
private positionHighlightBox(target: Node): void {
    const targetBounds = target.globalBounds;
    const parent = this.parent!;

    // 将全局像素坐标转换为 ScreenView 逻辑坐标
    const topLeft = parent.globalToLocalPoint(new Vector2(targetBounds.minX, targetBounds.minY));
    const bottomRight = parent.globalToLocalPoint(new Vector2(targetBounds.maxX, targetBounds.maxY));

    this.highlightBox.setRectBounds(new Bounds2(
        topLeft.x - this.highlightPadding,
        topLeft.y - this.highlightPadding,
        bottomRight.x + this.highlightPadding,
        bottomRight.y + this.highlightPadding
    ));
}

private positionContentBox(target: Node, preferredPosition: string): void {
    const targetBounds = target.globalBounds; // 先在全局（像素）空间计算位置
    const parent = this.parent!;
    // ... 计算 x, y（全局坐标）
    const localPoint = parent.globalToLocalPoint(new Vector2(x, y)); // 最后转换
    this.contentBox.x = localPoint.x;
    this.contentBox.y = localPoint.y;
}
```

---

### 0.2 Z-Index 与节点挂载时机盲区 (前端视角)

**现象**：引导遮罩、文字或高亮框被原本的业务节点（如可拖拽物体、图表区、控制面板）盖住，看不见或只能看见一半。

**规则**：PhET Scenery 的渲染顺序完全取决于 `this.addChild()` 的调用顺序（后加的在上层）。

```typescript
// ❌ 错误：在构造函数最前面加，会被后面的业务节点遮盖
this.tutorialOverlay = new TutorialOverlayNode(...);
this.addChild(this.tutorialOverlay); 
// ... 这里又 add 了控制面板和图表 ...

// ✅ 正确：必须确保 overlay 是 ScreenView 最后添加的子节点
// ... 其他业务节点初始化 ...
this.addChild(controlPanel);
// 必定最后加
this.addChild(this.tutorialOverlay);
```

---

### 0.3 Phet-io 遥测绕过原则 (框架视角)

组件中大量使用了 `tandem: Tandem.OPT_OUT`。

**为什么这样做**：PhET 核心框架对自带数据的组件（如 `TextPushButton`、`Rectangle`）要求极其严格的遥测（Phet-io）全量注册机制。如果没有 Opt-out，在严格环境中会抛出 `"Tandem validation failed"` 错误导致应用崩溃。

**注意点**：对于中文化的业务改造，我们**不要求支持**官方的 Phet-io 编辑层，因此统一使用 `Tandem.OPT_OUT` 是跳过严格状态机校验的有效捷径，能省下大量的样板代码。

---

### 1. 代码组织

```
your-sim/
├── js/
│   ├── common/
│   │   ├── model/
│   │   │   └── FeedbackManager.ts
│   │   └── view/
│   │       ├── TutorialOverlayNode.ts
│   │       ├── FeedbackToastNode.ts
│   │       ├── WelcomeDialog.ts
│   │       └── GameCardTooltipNode.ts
│   └── screens/
│       ├── screen1/
│       │   ├── view/
│       │   │   ├── Screen1View.ts
│       │   │   └── Screen1FeedbackRules.ts
│       └── screen2/
│           ├── view/
│           │   ├── Screen2View.ts
│           │   └── Screen2FeedbackRules.ts
```

### 2. 命名规范

```typescript
// 反馈规则文件
{ScreenName}FeedbackRules.ts  // e.g., MotionFeedbackRules.ts

// 规则导出
export const {screenName}FeedbackRules: FeedbackRule[] = [...];

// 规则 ID
'{screen-name}-{concept-name}'  // e.g., 'motion-force-creates-motion'

// 引导方法
private get{ScreenName}TutorialSteps(): TutorialStep[]
```

### 3. 性能优化

```typescript
// 避免在 check 函数中进行复杂计算
{
  id: 'bad-rule',
  check: (model) => {
    // 不好：每次都计算复杂表达式
    return model.items.reduce((sum, item) => sum + item.value, 0) > threshold;
  }
}

{
  id: 'good-rule',
  check: (model) => {
    // 好：使用简单的属性比较
    return model.totalValueProperty.value > threshold;
  }
}

// 使用 DerivedProperty 缓存计算结果
const totalValueProperty = new DerivedProperty(
  [model.items],
  items => items.reduce((sum, item) => sum + item.value, 0)
);
```

### 4. 调试技巧

```typescript
// 添加调试日志
private update(model: any): void {
  console.log('FeedbackManager.update() called');
  console.log('Triggered rules:', Array.from(this.triggeredRules));
  console.log('Available rules:', this.feedbackRules.filter(r => r.check(model)));

  // 原有逻辑...
}

// 测试特定规则
feedbackManager.resetRule('rule-id');
feedbackManager.update(model);
```

### 5. 国际化处理

```typescript
// 好的做法：使用字符串文件
{
  id: 'good-rule',
  message: YourSimStrings.feedback.ruleIdStringProperty,
  // ...
}

// 避免硬编码字符串（除非是项目特定语言）
{
  id: 'acceptable-for-chinese-only',
  message: '直接使用中文',
  // ...
}
```

---

## 常见问题

### Q1: 引导显示位置不正确 / 遮罩只覆盖一半

**问题**: 高亮框只框住了目标区域的一半；或遮罩只暗化了左侧/右侧一半屏幕。

**根本原因**：坐标转换用了 `this.globalToLocalPoint` 而非 `this.parent.globalToLocalPoint`。

**解决方案**:

1. **检查坐标转换**：`positionHighlightBox` 和 `positionContentBox` 必须通过 `this.parent!.globalToLocalPoint()` 转换，而非 `this.globalToLocalPoint()`
2. **检查遮罩方案**：如使用四面板切割，建议改为单层 `backdrop` Rectangle（全屏覆盖 + 透明高亮边框叠加）
3. **检查 `layoutBounds`**：是否正确传递，默认 `new Bounds2(0, 0, 1024, 618)`
4. **确保节点已加入显示树**：在 `setTimeout` 回调中调用 `restart()`，等待布局完成
5. 使用 `position` 参数调整内容框位置

### Q2: 反馈没有触发

**问题**: 规则条件满足但反馈没有显示。

**排查步骤**:
1. 检查规则是否已注册
2. 检查 `enabledProperty` 是否为 true
3. 检查是否处于冷却期
4. 检查规则是否已触发过（使用 `hasTriggered()`）
5. 添加调试日志查看 `update()` 是否被调用

### Q3: 反馈频繁触发造成干扰

**问题**: 反馈消息出现太频繁。

**解决方案**:
- 调整优先级，确保重要消息优先
- 增加冷却时间（修改 `TYPE_COOLDOWNS`）
- 优化规则条件，避免重叠触发
- 使用规则级别的一次性触发机制

### Q4: 模型没有 stepEmitter

**问题**: 某些模型（如 NetForceModel）没有 `stepEmitter`。

**解决方案**:
```typescript
// 使用 Multilink 监听多个属性
Multilink.multilink([
  model.property1,
  model.property2,
  model.property3
], () => {
  this.feedbackManager.update(model);
});
```

### Q5: TypeScript 类型错误

**问题**: 类型定义不匹配。

**解决方案**:
```typescript
// 使用类型断言
check: (model: any) => {
  const typedModel = model as YourModelType;
  return typedModel.property.value > threshold;
}

// 或使用泛型（需要修改 FeedbackManager）
check: (model: YourModelType) => {
  return model.property.value > threshold;
}
```

### Q6: 高亮框对齐空白区域（框选了不存在的内容）

**问题**: 橙色高亮框出现在空白区域，目标节点内没有可见内容。

**常见原因**：

| 节点类型 | 问题原因 | 解决方案 |
|----------|----------|----------|
| `trackSelectionNode` / `sceneSelectionNode` | 某些屏幕根本没有这个节点（始终为 `null`） | 删掉对应步骤或换成真实存在的节点 |
| 默认隐藏的节点（如饼图图例） | 节点存在但不可见，框选到空白 | 在 `onShow` 回调中主动设置 `model.xxxVisibleProperty.value = true` |
| Canvas 精灵（如 `skaterNode`） | 全局坐标不稳定，每帧偏移 | 改用其静态的父容器节点 |
| 动态节点（如第一个创建的对象） | 还未创建时 target 函数返回 null | 改用稳定存在的容器节点，或延长引导启动延迟 |

**检查方式**：在 ScreenView 构造函数完成后，打印 `this.xxx?.globalBounds`，查看是否为有效边界。

### Q7: 引导步骤内容弹窗与框选区域重叠

**问题**: 内容卡片出现在橙色高亮框的正中间，遮挡了要介绍的内容。

**解决方案**：

```typescript
// 根据目标节点位置选择合理的 position
{
  target: this.controlPanel,  // 控制面板在屏幕右侧
  position: 'left'            // ← 内容卡片显示在左侧
}

{
  target: this.graphBox,  // 图形在屏幕上方
  position: 'bottom'      // ← 内容卡片显示在下方
}
```

**选择原则**：`position` 应指向「目标节点对面」的空白区域，给卡片足够空间不与高亮框交叉。

---

## 风险提示

### ⚠️ 框架级别修改风险

修改 `joist/` 和 `brand/` 会影响整个 PhET 项目中的所有模拟器，请谨慎操作。

#### joist/ 修改影响

| 文件 | 影响范围 | 风险等级 |
|------|----------|----------|
| `Sim.ts` | 所有使用 Sim 类的模拟器 | 🟡 中 |
| `HomeScreenView.ts` | 所有有首页的模拟器 | 🟡 中 |
| `NavigationBarScreenButton.ts` | 所有有导航栏的模拟器 | 🟢 低 |

**建议**：
- 在修改前先提交当前代码
- 在单独的分支上进行修改
- 修改后测试多个模拟器确保没有破坏性变更

#### brand/ 修改影响

| 修改类型 | 影响范围 |
|----------|----------|
| logo 图片 | 仅使用 `adapted-from-phet` 品牌的模拟器 |
| license.json | 仅影响许可证声明显示 |

**建议**：
- 保留原始 logo 备份文件
- 如果只是临时修改，考虑使用环境变量或配置开关

### ⚠️ babel/ 修改风险

修改 `babel/forces-and-motion-basics/forces-and-motion-basics-strings_zh_CN.json` 会影响所有使用该语言的模拟器。

**建议**：
- 修改后运行 `grunt modulify --targets=strings` 同步字符串
- 测试中文界面显示是否正常

---

## 快速开始检查清单

### 最小化改造（仅添加引导和反馈）

如果你只想添加引导和反馈功能，不修改首页和 logo，只需要：

- [ ] 复制 3 个核心组件：TutorialOverlayNode.ts, FeedbackToastNode.ts, FeedbackManager.ts
- [ ] 修改命名空间注册
- [ ] 为每个屏幕创建反馈规则文件
- [ ] 修改每个 ScreenView 集成引导和反馈
- [ ] 更新项目内的字符串文件
- [ ] 运行 `grunt type-check` 和 `grunt lint`

### 完整改造（包括首页和品牌）

如果要实现与 forces-and-motion-basics 完全一样的效果，还需要：

- [ ] 复制 WelcomeDialog.ts 和 GameCardTooltipNode.ts
- [ ] 修改 `joist/js/Sim.ts` 添加自定义 HomeScreen 支持
- [ ] 修改 `joist/js/HomeScreenView.ts` 添加欢迎对话框
- [ ] 修改 `joist/js/NavigationBarScreenButton.ts` 注释宽度断言
- [ ] 修改 `babel/forces-and-motion-basics/forces-and-motion-basics-strings_zh_CN.json`
- [ ] 修改 `brand/adapted-from-phet/images/` 下的 logo 文件
- [ ] 运行 `grunt modulify --targets=strings`

---

## 检查清单

### 改造完成检查

#### 基础功能
- [ ] 所有公共组件已复制并修改命名空间
- [ ] 每个屏幕都有反馈规则文件
- [ ] 每个屏幕的 ScreenView 都已修改
- [ ] 字符串文件已更新
- [ ] 代码通过类型检查 (`grunt type-check`)
- [ ] 代码通过 lint 检查 (`grunt lint`)

#### 引导系统
- [ ] 引导步骤定义完整
- [ ] 每个步骤的目标元素正确
- [ ] 引导内容清晰易懂
- [ ] 引导完成回调正确设置
- [ ] 引导延迟显示正常工作

#### 反馈系统
- [ ] 反馈规则覆盖主要交互
- [ ] 规则优先级设置合理
- [ ] 冷却机制正常工作
- [ ] 消息文本准确无误
- [ ] 重置功能清空所有状态

#### 用户体验
- [ ] 引导和反馈不会互相干扰
- [ ] 反馈位置不遮挡重要内容
- [ ] 文本显示无乱码
- [ ] 动画流畅无卡顿
- [ ] 所有屏幕功能正常

### 代码质量检查

- [ ] 遵循项目编码规范
- [ ] 所有公共方法有 JSDoc 注释
- [ ] 无 console.log 或调试代码残留
- [ ] 无硬编码的魔法数字
- [ ] 适当的错误处理

### 测试检查

- [ ] 在 Chrome 中测试
- [ ] 在 Firefox 中测试
- [ ] 在 Safari 中测试
- [ ] 在移动设备上测试
- [ ] 不同屏幕尺寸下测试

---

## 附录：forces-and-motion-basics 引导步骤详解

### 运动屏幕 (Motion Screen) - 4步

```
────────────────────────────────────────
步骤: 1
标题: 欢迎使用运动实验室！
目标元素: appliedForceControl (施力滑块)
说明: 拖动滑块向右或向左施加推力，观察箱子如何运动。
位置: right
────────────────────────────────────────
步骤: 2
标题: 添加物体
目标元素: itemToolboxGroup (工具箱)
说明: 从工具箱拖动不同物体到滑板上，改变总质量，观察对运动的影响。
位置: top
────────────────────────────────────────
步骤: 3
标题: 测量速度
目标元素: speedometerNode (速度计)
说明: 速度计显示物体的实时速度，单位是 m/s。
位置: left
回调: onShow 时显示速度，onHide 时隐藏速度
────────────────────────────────────────
步骤: 4
标题: 显示数据
目标元素: controlPanel (控制面板)
说明: 在控制面板中勾选不同的选项，可以显示力、速度、质量等数值。
位置: left
回调: onShow 时显示数值选项
```

### 摩擦力屏幕 (Friction Screen) - 4步

```
────────────────────────────────────────
步骤: 1
标题: 欢迎使用摩擦力实验室！
目标元素: controlPanel (控制面板)
说明: 拖动滑块改变摩擦系数，观察不同表面的摩擦力大小。
位置: left
回调: onShow 时显示控制面板
────────────────────────────────────────
步骤: 2
标题: 观察摩擦力
目标元素: itemStackGroup (物体堆)
说明: 注意物体下方出现的黄色箭头，它代表摩擦力的方向和大小。
位置: bottom
回调: onShow 时显示摩擦力箭头
────────────────────────────────────────
步骤: 3
标题: 施加推力
目标元素: appliedForceControl (施力滑块)
说明: 尝试施加推力，观察静摩擦力如何抵抗你的推力直到物体开始运动。
位置: right
────────────────────────────────────────
步骤: 4
标题: 显示数值
目标元素: controlPanel (控制面板)
说明: 勾选"数值"选项，可以精确看到力、速度、质量等数据。
位置: left
回调: onShow 时显示数值选项
```

### 加速度屏幕 (Acceleration Screen) - 6步

```
────────────────────────────────────────
步骤: 1
标题: 欢迎使用加速度实验室！
目标元素: appliedForceControl (施力滑块)
说明: 拖动滑块施加推力，观察力如何改变物体的运动状态。
位置: right
────────────────────────────────────────
步骤: 2
标题: 改变质量
目标元素: itemToolboxGroup (工具箱)
说明: 从工具箱拖动不同物体到滑板上，改变总质量。
位置: top
────────────────────────────────────────
步骤: 3
标题: 观察速度计
目标元素: speedometerNode (速度计)
说明: 速度计显示物体的实时速度，单位是 m/s。
位置: left
回调: onShow 时显示速度，onHide 时隐藏速度
────────────────────────────────────────
步骤: 4
标题: 观察加速度计
目标元素: accelerometerNode (加速度计)
说明: 加速度计显示物体的加速度，单位是 m/s²。加速度反映速度变化的快慢。
位置: left
────────────────────────────────────────
步骤: 5
标题: 合力与加速度
目标元素: 包含加速度计和物体的容器
说明: 力越大，加速度越大；质量越大，加速度越小。这就是 F = ma！
位置: bottom
────────────────────────────────────────
步骤: 6
标题: 显示数据
目标元素: controlPanel (控制面板)
说明: 勾选"加速度"选项，可以显示加速度的具体数值。
位置: left
回调: onShow 时显示数值选项
```

### 合力屏幕 (Net Force Screen) - 5步

```
────────────────────────────────────────
步骤: 1
标题: 欢迎使用拔河游戏！
目标元素: leftToolbox (左侧工具箱)
说明: 将蓝色或红色的小人从工具箱拖到绳子的打结处，开始拔河吧。左边是蓝队，右边是红队。
位置: bottom
────────────────────────────────────────
步骤: 2
标题: 添加拔河队员
目标元素: 动态目标 (第一个已连接的拔河队员或工具箱)
说明: 从工具箱拖动小人到绳子上的结点，添加你的第一个拔河队员。
位置: bottom
────────────────────────────────────────
步骤: 3
标题: 开始游戏
目标元素: goPauseButton (开始/暂停按钮)
说明: 选好队员后，点击"开始"按钮开始拔河比赛。
位置: top
────────────────────────────────────────
步骤: 4
标题: 观察力的合成
目标元素: cartNode (小车)
说明: 绿色虚线箭头显示所有力的总和。当两边的力相等时，合力为 0，小车不会移动。
位置: bottom
回调: onShow 时显示合力，onHide 时隐藏合力
────────────────────────────────────────
步骤: 5
标题: 显示数值
目标元素: controlPanel (控制面板)
说明: 勾选"数值"可以看到每个力的具体数值，方便你精确计算。
位置: left
回调: onShow 时显示数值选项 (引导结束后保持显示)
```

---

## 附录：外部配置文件修改

除了项目主目录的修改，还需要修改以下外部配置文件：

### 1. 多语言配置文件 (`babel/`)

**文件路径**: `babel/forces-and-motion-basics/forces-and-motion-basics-strings_zh_CN.json`

**修改内容**: 添加中文本地化字符串，用于欢迎对话框和游戏卡片描述。

```json
{
  "welcome": {
    "value": "欢迎"
  },
  "netForce.description": {
    "value": "合力拔河：探索力的合成与平衡，在激烈的拔河比赛中学习力的相互作用"
  },
  "motion.description": {
    "value": "运动基础：理解力如何影响物体的运动状态，探索力与加速度的关系"
  },
  "friction.description": {
    "value": "摩擦力：发现摩擦力如何阻碍运动，探索不同表面的摩擦特性"
  },
  "acceleration.description": {
    "value": "加速度：深入研究牛顿第二定律，理解力、质量与加速度的数学关系"
  },
  "start.exploration": {
    "value": "开始探索"
  }
}
```

---

### 1.1 快速开发与调试手段 (QA 视角)

在实施引导系统开发时，如果你需要频繁刷新并触发第一屏引导，可以用以下浏览器控制台指令大幅提升效率：

```javascript
// 清理引导缓存并刷新当前页面
localStorage.clear();
sessionStorage.clear();
location.reload();
```

*建议使用 Chrome 无痕模式进行开发测试，每次关掉窗口即自动清空 `localStorage`。*

---

### 1.2 边缘用例测试 (Edge Cases 视角)

在提交前，请务必进行以下**极端场景**测试：

1. **浏览器窗口 Resize 测试**：在显示引导框和高亮边框时，改变浏览器窗口大小（包含最大化、恢复窗口），验证高亮框是否能跟随目标节点偏移。
2. **极端比例拉伸（带宽屏/竖屏测试）**：将浏览器模拟为超宽或极窄比例，确认深色遮罩（backdrop）是否由于尺寸不足而在边缘露出非暗化区域（参考经验五）。

---

### 2. 品牌与 Logo 配置 (`brand/`)

**文件路径**: `brand/adapted-from-phet/images/`

**修改内容**: 替换 PhET 默认 logo 为自定义 logo。

#### 2.1 图片文件

| 文件 | 说明 |
|------|------|
| `logo.png` | 主 logo（深色背景用） |
| `logoOnWhite.png` | 白色背景 logo |
| `splash.svg` | 启动画面 SVG |

**操作**: 将自定义 logo 文件替换上述文件。

#### 2.2 TypeScript 导出文件

| 文件 | 说明 |
|------|------|
| `logo_png.ts` | logo.png 的 Base64 导出 |
| `logoOnWhite_png.ts` | logoOnWhite.png 的 Base64 导出 |
| `splash_svg.ts` | splash.svg 的 Base64 导出 |

**注意**: 这些文件包含图片的 Base64 编码，替换图片后需要重新生成。

#### 2.3 许可证文件 (`license.json`)

```json
{
  "logo.png": {
    "text": ["Copyright Shenzhen International Science and Technology Information Center"],
    "projectURL": "https://itic-sci.com",
    "license": "ITIC",
    "notes": "Replaced with ITIC logo"
  },
  "logoOnWhite.png": {
    "text": ["Copyright Shenzhen International Science and Technology Information Center"],
    "projectURL": "https://itic-sci.com",
    "license": "ITIC",
    "notes": "Replaced with ITIC logo"
  },
  "splash.svg": {
    "text": ["Copyright Shenzhen International Science and Technology Information Center"],
    "projectURL": "https://itic-sci.com",
    "license": "ITIC",
    "notes": "Replaced with ITIC logo"
  }
}
```

#### 2.4 备份文件

建议创建备份文件夹 `backup/` 保存原始文件：
- `backup/logo.png.bak`
- `backup/logoOnWhite.png.bak`
- `backup/splash.svg.bak`

---

### 3. 框架级别修改 (`joist/`)

**文件路径**: `joist/js/`

**修改目的**: 为首页添加自定义主题和欢迎对话框支持。

#### 3.1 Sim.ts

**修改内容**: 添加自定义 HomeScreen 工厂函数支持。

```typescript
// 在 SelfOptions 类型中添加
type SelfOptions = {
  // ... 其他选项

  // 可选的自定义 HomeScreen 工厂函数
  createHomeScreen?: ( screens: AnyScreen[], homeScreenTandem: Tandem, simInstance: Sim ) => HomeScreen;
};
```

```typescript
// 在创建 HomeScreen 时使用
// 创建 homeScreen tandem 供 createHomeScreen 使用
const homeScreenTandem = options.tandem.createTandem( PhetioIDUtils.HOME_SCREEN_COMPONENT_NAME );

// ...

// 使用自定义 createHomeScreen（如果提供），否则使用默认 HomeScreen
if ( options.createHomeScreen ) {
  return options.createHomeScreen( selectedSimScreens, homeScreenTandem, this );
}
return new HomeScreen( this.simNameProperty, () => this.selectedScreenProperty, selectedSimScreens, /* ... */ );
```

#### 3.2 HomeScreenView.ts

**修改内容**: 添加自定义主题检测和欢迎对话框功能。

```typescript
// 检测是否为需要自定义主题的模拟器
const simName = simNameProperty.value || '';
const isCustomTheme = simName.includes( '力' ) ||
                     simName.includes( 'Forces and Motion' ) ||
                     simName.includes( 'forces-and-motion' );

// 如果启用自定义主题，使用深色背景并显示欢迎对话框
if ( isCustomTheme ) {
  const bgRect = new Rectangle( 0, 0, this.layoutBounds.width, this.layoutBounds.height, {
    fill: '#1a1a2e'
  } );
  this.addChild( bgRect );

  // 页面刷新时清除 sessionStorage 标记
  const navigationEntries = performance.getEntriesByType( 'navigation' );
  if ( navigationEntries.length > 0 ) {
    const navType = navigationEntries[0].type;
    if ( navType === 'reload' ) {
      sessionStorage.removeItem( 'phet-welcome-dialog-closed' );
    }
  }

  // 显示欢迎对话框（仅第一次）
  this.showWelcomeDialog();
}

// 根据主题选择标题颜色
const titleColor = isCustomTheme ? '#00f2ff' : 'white';
```

#### 3.3 NavigationBarScreenButton.ts

**修改内容**: 注释掉宽度断言，因为自定义 HomeScreen 可能有更长的屏幕名称。

```typescript
// 原代码
// assert && needsIconMaxWidth && assert( toFixed( this.width, 0 ) === toFixed( options.maxButtonWidth!, 0 ),
//   `this.width ${this.width} !== options.maxButtonWidth ${options.maxButtonWidth}` );

// 修改后
// NOTE: Assertion removed because CustomHomeScreen may have longer names that exceed maxButtonWidth
// assert && needsIconMaxWidth && assert( toFixed( this.width, 0 ) === toFixed( options.maxButtonWidth!, 0 ),
//   `this.width ${this.width} !== options.maxButtonWidth ${options.maxButtonWidth}` );
```

#### 3.4 HomeScreenButton (游戏卡片悬浮提示)

**注意**: 这是在项目内部修改，不是框架修改。但与 GameCardTooltipNode 组件配合使用。

**文件路径**: `your-sim/js/home-screen/view/HomeScreenButton.ts`（或类似路径）

**修改内容**: 添加 GameCardTooltipNode 集成，实现悬浮时显示游戏说明。

```typescript
import GameCardTooltipNode from '../../common/view/GameCardTooltipNode.js';
import YourSimStrings from '../../YourSimStrings.js';

export default class HomeScreenButton extends ScreenButton {

  private readonly tooltipNode: GameCardTooltipNode;

  public constructor( providedOptions: HomeScreenButtonOptions ) {
    // ... 原有构造函数代码 ...

    // 创建提示节点（根据屏幕名称设置不同的描述）
    this.tooltipNode = new GameCardTooltipNode( {
      title: this.getScreenTitle(),
      description: this.getScreenDescription(),
      visible: false,
      opacity: 0
    } );

    // 添加到显示列表
    this.addChild( this.tooltipNode );

    // 添加悬浮监听
    this.addInputListener( {
      enter: () => {
        // 在全局坐标系中定位提示
        this.tooltipNode.positionRelativeTo( this, this.layoutBounds );
        this.tooltipNode.show();
      },
      exit: () => {
        this.tooltipNode.hide();
      }
    } );
  }

  private getScreenTitle(): string {
    // 根据屏幕返回标题
    const screenName = this.screen.nameProperty.value;
    if ( screenName.includes( 'netForce' ) ) {
      return '合力';
    } else if ( screenName.includes( 'motion' ) ) {
      return '运动';
    }
    // ... 其他屏幕
    return screenName;
  }

  private getScreenDescription(): string {
    // 从字符串文件获取描述
    const screenName = this.screen.nameProperty.value;
    const key = `${screenName}.description`;
    return YourSimStrings[key]?.value || '';
  }
}
```

**注意**: HomeScreenButton 的实际路径和实现可能因项目而异，需要根据具体项目调整。

---

### 4. 修改文件总览

| 位置 | 文件 | 修改类型 | 必需性 |
|------|------|----------|--------|
| `babel/forces-and-motion-basics/` | `forces-and-motion-basics-strings_zh_CN.json` | 添加中文字符串 | 必需（中文版） |
| `brand/adapted-from-phet/images/` | `logo.png`, `logoOnWhite.png`, `splash.svg` | 替换 logo | 可选 |
| `brand/adapted-from-phet/images/` | `logo_png.ts`, `logoOnWhite_png.ts`, `splash_svg.ts` | 更新 Base64 | 必需（如替换 logo） |
| `brand/adapted-from-phet/images/` | `license.json` | 更新许可证 | 必需（如替换 logo） |
| `joist/js/` | `Sim.ts` | 添加工厂函数支持 | 可选（自定义首页） |
| `joist/js/` | `HomeScreenView.ts` | 添加欢迎对话框 | 可选（自定义首页） |
| `joist/js/` | `NavigationBarScreenButton.ts` | 注释断言 | 可选（自定义首页） |
| `your-sim/js/` | `HomeScreenButton.ts` | 添加悬浮提示 | 可选（游戏卡片提示） |

---

## 附录：完整示例

### 示例项目结构

```
circuit-construction-kit-dc/
├── js/
│   ├── common/
│   │   ├── model/
│   │   │   └── FeedbackManager.ts
│   │   └── view/
│   │       ├── TutorialOverlayNode.ts
│   │       └── FeedbackToastNode.ts
│   ├── dc/
│   │   ├── view/
│   │   │   ├── DCScreenView.ts (修改)
│   │   │   └── DCFeedbackRules.ts (新增)
│   └── ...
└── circuit-construction-kit-dc-strings_en.yaml (修改)
```

### 示例反馈规则

```typescript
// DCFeedbackRules.ts
import type DCModel from '../../model/DCModel.js';
import { FeedbackRule } from '../../../common/model/FeedbackManager.js';

export const dcFeedbackRules: FeedbackRule[] = [
  {
    id: 'first-component-placed',
    check: (model: DCModel) => {
      return model.circuit.components.length === 1;
    },
    message: '太好了！你放置了第一个元件。',
    type: 'discovery',
    priority: 0
  },
  {
    id: 'circuit-completed',
    check: (model: DCModel) => {
      return model.circuit.isComplete() && model.circuit.hasPowerSource();
    },
    message: '电路完成了！电流开始流动。',
    type: 'success',
    priority: 10
  },
  {
    id: 'short-circuit-detected',
    check: (model: DCModel) => {
      return model.circuit.isShortCircuit();
    },
    message: '检测到短路！检查一下你的电路连接。',
    type: 'discovery',
    priority: 5
  }
];
```

### 示例引导步骤

```typescript
// DCScreenView.ts
private getTutorialSteps(): TutorialStep[] {
  return [
    {
      target: this.toolbox,
      title: '欢迎使用电路实验室！',
      content: '从工具箱拖动元件到工作区，开始搭建你的电路。',
      position: 'right'
    },
    {
      target: () => this.getFirstComponentNode(),
      title: '添加第一个元件',
      content: '拖动一个电池到工作区，作为电源。',
      position: 'bottom'
    },
    {
      target: this.wireTool,
      title: '连接导线',
      content: '使用导线工具连接元件，形成完整电路。',
      position: 'left'
    },
    {
      target: this.playButton,
      title: '开始模拟',
      content: '点击播放按钮，观察电流如何流动。',
      position: 'top'
    }
  ];
}
```

---

## 版本历史

- v1.0 (2025-02): 初始版本，基于 forces-and-motion-basics 改造经验
- v1.1 (2026-03): 补充 Energy Skate Park 移植案例，新增坐标转换规则、遮罩方案选择、节点稳定性原则、Q6/Q7 常见问题
- v1.2 (2026-03-04): 补充 ESP 完整验证后的工程细节：超大矩形遮罩、super() 顺序、「完成」按钮双按钮方案、暴露子节点最佳实践
- v1.3 (2026-03-04): 增加多视角（架构、前端、框架、QA）深度避坑指南，包括 Z-Index 盲区、前端布局异步陷阱、Phet-io 绕过策略以及 QA 测试捷径。

---

## Energy Skate Park 移植案例与经验沉淀

> 本章节记录将 TutorialOverlayNode 从 `forces-and-motion-basics` 移植到 `energy-skate-park` 时发现的工程问题，供后续项目参考。

### 移植背景

`energy-skate-park` 有四个屏幕（入门/测量/图形/游乐场），结构比 `forces-and-motion-basics` 复杂，模型、视图节点关系更深，布局更多变。

### 经验一：坐标系转换必须用 `parent.globalToLocalPoint`

**现象**：高亮框只框住了目标区域的一半；遮罩只暗化了左侧或右侧一半屏幕。

**根因**：`positionHighlightBox` 里用了 `this.globalToLocalPoint`，在某些 PhET ScreenView scale 配置下，会导致坐标差 1/2。

**修复**：改为 `this.parent!.globalToLocalPoint`，与参考实现完全一致。

```typescript
// 正确写法（与 forces-and-motion-basics 一致）
const parent = this.parent!;
const topLeft = parent.globalToLocalPoint(new Vector2(targetBounds.minX, targetBounds.minY));
const bottomRight = parent.globalToLocalPoint(new Vector2(targetBounds.maxX, targetBounds.maxY));
```

---

### 经验二：放弃四面板切割，使用单层 backdrop 方案

**现象**：四面板方案在坐标计算稍有偏差时，面板之间出现缝隙，遮罩效果损坏。

**修复**：改为一个全屏 `backdrop` Rectangle（1024×618，半透明黑色），加上一个透明内部的高亮边框，与参考实现完全一致。这样无论坐标如何变化，全屏覆盖都是可靠的。

---

### 经验三：引导目标节点的稳定性原则

移植时发现这些节点不适合作为引导目标：

| 节点名 | 问题 | 处理方法 |
|--------|------|----------|
| `trackSelectionNode` | 部分屏幕不存在该节点，getter 返回 null | 移除对应步骤，换用 `controlPanel` |
| `sceneSelectionNode` | 同上 | 同上 |
| `skaterNode` | 是 Canvas 精灵，globalBounds 每帧动态变化 | 改用其父容器或不使用此类节点 |
| 默认隐藏节点（如饼图图例） | 节点存在但内容不可见，框选空白 | 在 `onShow` 回调中执行 `model.pieChartVisibleProperty.value = true` |

**推荐的稳定目标节点**（始终存在且布局稳定）：
- `this.controlPanel` — 右侧控制面板，全屏幕共有
- `this.pieChartLegend` — 饼图图例（需配合 onShow 开启）
- `this.trackLayer` — 轨道容器
- `this.graphAccordionBox` — 图形屏幕的折叠图表盒
- `this.trackToolbox` — 游乐场屏幕轨道工具箱

---

### 经验四：引导步骤第一步统一用全屏模式

每个屏幕的第一步引导，使用 `target: null`（全屏遮罩居中弹窗），作为欢迎介绍，不框选任何具体控件：

```typescript
[
  {
    target: null,      // ← 全屏遮罩，无高亮框
    title: '欢迎来到能量滑板公园！',
    content: '这里你可以研究能量转换...',
    position: 'center'
  },
  {
    target: this.pieChartLegend,          // ← 第二步再框选具体控件
    title: '能量分布图',
    content: '勾选「饼图」...',
    position: 'right',
    onShow: () => { model.pieChartVisibleProperty.value = true; }
  },
  // ...
]
```

---

### Energy Skate Park 四屏引导步骤参考

#### 入门屏幕 (Intro) — 5步

| 步骤 | 目标节点 | 标题 | 内容弹窗位置 |
|------|----------|------|--------------|
| 1 | `null`（全屏） | 欢迎来到能量滑板公园！ | center |
| 2 | `pieChartLegend` + onShow开启饼图 | 能量分布图 | right |
| 3 | `controlPanel`（上半部分指标区） | 打开速度与饼图指标 | left |
| 4 | `controlPanel`（整体） | 调整重力与质量 | left |
| 5 | `trackLayer` | 探索不同轨道形状 | top |

#### 测量屏幕 (Measure) — 4步

| 步骤 | 目标节点 | 标题 | 内容弹窗位置 |
|------|----------|------|--------------|
| 1 | `null` | 欢迎来到测量屏幕！ | center |
| 2 | `pieChartLegend` + onShow | 观察能量分布 | right |
| 3 | `pathSensor`（数据传感器） | 数据传感器 | right |
| 4 | `controlPanel` | 调整参数 | left |

#### 图形屏幕 (Graphs) — 4步

| 步骤 | 目标节点 | 标题 | 内容弹窗位置 |
|------|----------|------|--------------|
| 1 | `null` | 欢迎来到图形分析屏幕！ | center |
| 2 | `graphAccordionBox` | 能量-时间图 | top |
| 3 | `variableSwitch \|\| graphAccordionBox` | 切换图形变量 | top |
| 4 | `controlPanel` | 调整参数 | left |

#### 游乐场屏幕 (Playground) — 4步

| 步骤 | 目标节点 | 标题 | 内容弹窗位置 |
|------|----------|------|--------------|
| 1 | `null` | 欢迎来到游乐场！ | center |
| 2 | `trackToolbox` | 轨道工具箱 | top |
| 3 | `clearButton` | 清空轨道 | top |
| 4 | `controlPanel` | 调整参数 | left |

---

### 经验五：宽屏下遮罩需使用超大矩形，而非 layoutBounds

**现象**：模拟器在宽屏浏览器（>1024px）中，`backdrop` 只覆盖 layoutBounds（1024×618）范围，右侧浮动控制面板不在遮罩内，整体遮罩效果残破。

**原因**：PhET ScreenView 的 `layoutBounds` 固定为 `1024×618`，但浏览器实际宽度可能远大于此，右侧控制面板通过 `visibleBoundsProperty` 被锚定到真实屏幕右边缘，超出了 layoutBounds。

**修复**：将 `backdrop` 改为超大矩形（边长20000），以 `(0,0)` 为中心，覆盖任意分辨率：

```typescript
// ✅ 正确：超大矩形确保任意分辨率均覆盖
const MASK_EXTENT = 10000;
this.backdrop = new Rectangle(-MASK_EXTENT, -MASK_EXTENT, MASK_EXTENT * 2, MASK_EXTENT * 2, {
    fill: 'rgba(0, 0, 0, 0.72)',
    pickable: true
});

// ❌ 不够：只覆盖 layoutBounds，宽屏下两侧无遮罩
this.backdrop = new Rectangle(0, 0, layoutBounds.width, layoutBounds.height, { ... });
```

**适用时机**：当模拟器的控制面板使用 `visibleBoundsProperty` 锚定到实际屏幕边缘（常见于宽屏适配设计）时，必须用超大矩形；若控制面板完全在 layoutBounds 内，则 layoutBounds 即可。

---

### 经验六：派生类构造函数 `super()` 必须在 `this.xxx =` 之前调用

**错误现象**：打开 HTML 报错：
```
Uncaught ReferenceError: Must call super constructor in derived class before
accessing 'this' or returning from derived constructor
```

**根因**：为了暴露内部节点（如 `visibilityControlsNode`、`experimentSettingsNode`），在 `super()` 调用之前对 `this` 进行了赋值：

```typescript
// ❌ 错误：super() 之前访问 this
public readonly visibilityControlsNode: Node;
public readonly experimentSettingsNode: Node;

public constructor(...) {
    this.visibilityControlsNode = new VBox(...);  // 💥 super() 还没调用！
    super(...);
}
```

**修复**：先调用 `super()`，再赋值：

```typescript
// ✅ 正确：super() 之后再赋值
public constructor(...) {
    super(...);
    // 先创建局部变量
    const visibilityControls = new VBox(...);
    this.visibilityControlsNode = visibilityControls;
    // 再把局部变量传给 children
    super.addChild(visibilityControls);
}
```

---

### 经验七：引导最后一步按钮切换 — 用两个按钮代替动态改文字

**现象**：引导进行到最后一步，按钮文字应从「下一步」切换为「完成」，但按钮仍显示「下一步」。

**根因**：尝试通过 `(this.nextButton as any)._label.string = '完成'` 修改按钮内部私有文字节点，这是 hacky 做法，且 `TextPushButton` 的内部实现不保证 `._label` 属性存在。

**修复**：声明两个独立按钮，用 `visible` 切换：

```typescript
// 声明两个按钮
private readonly nextButton: TextPushButton;     // «下一步»
private readonly completeButton: TextPushButton; // «完成»

// 构建时：
this.completeButton = new TextPushButton('完成', {
    listener: () => this.complete()
    // ... 同 nextButton 样式
});
this.completeButton.visible = false;

// children 同时包含两个按钮：
this.buttonContainer = new HBox({
    children: [this.previousButton, this.skipButton, this.nextButton, this.completeButton]
});

// showStep 中切换显示：
const isLastStep = index === this.steps.length - 1;
this.nextButton.visible = !isLastStep;
    this.completeButton.visible = isLastStep;
this.skipButton.visible = !isLastStep;
```

**通用原则**：PhET Scenery 中任何按钮的文字一旦创建就不应动态修改；需要文字切换时，用两个按钮 + `visible` 控制是最稳定的做法。

---

### 经验八：暴露控制面板子节点以实现精确高亮

**现象**：Intro 屏幕第3步和第4步都框选了同一个区域（整个 `controlPanel`），无法区分「速度/饼图勾选区」和「重力/质量滑块区」。

**根因**：`EnergySkateParkControlPanel` 内部创建了 `visibilityControls`（勾选区）和 `experimentSettingsNode`（滑块区），但都是局部变量，外部无法访问。

**修复**：在面板类中声明 `public readonly` 属性暴露这些子节点：

```typescript
// EnergySkateParkControlPanel.ts
export class EnergySkateParkControlPanel extends Panel {
    public readonly visibilityControlsNode: Node;   // 饼图/速度勾选区
    public readonly experimentSettingsNode: Node;   // 重力/质量调节区

    public constructor(...) {
        super(...);
        this.visibilityControlsNode = visibilityControls;
        this.experimentSettingsNode = experimentSettings;
    }
}

// IntroScreenView.ts — 分别指向不同子节点
{
    target: controlPanel.visibilityControlsNode,  // 步骤3：勾选区
    title: '打开速度与饼图指标',
    ...
},
{
    target: controlPanel.experimentSettingsNode,  // 步骤4：滑块区
    title: '调整重力与质量',
    ...
}
```

**通用原则**：移植时如需精确高亮某个控制面板内的子区域，应在面板类中将其暴露为 `public readonly` 而非访问私有属性。

---

### 经验九：多屏幕引导的 localStorage 持久化

每个屏幕的引导应独立记录「已看过」状态，避免切换屏幕后引导不出现。建议使用屏幕名作为 key 的一部分：

```typescript
// 入门屏幕
const STORAGE_KEY = 'esp-tutorial-seen-intro';

// 测量屏幕
const STORAGE_KEY = 'esp-tutorial-seen-measure';

// 图形屏幕
const STORAGE_KEY = 'esp-tutorial-seen-graphs';

// 游乐场屏幕
const STORAGE_KEY = 'esp-tutorial-seen-playground';
```

重新访问时（页面刷新），使用 `localStorage.removeItem(key)` 或 `localStorage.clear()` 清除标记，让引导重新出现。

> **注意**：`sessionStorage` 在标签页关闭后清除，`localStorage` 跨会话持久。根据需求选择：
> - 教育场景（希望每次刷新后重现引导）→ 用 `localStorage` + 刷新时主动清除
> - 单次会话引导 → 用 `sessionStorage`

---

## 许可与贡献

本指南基于 PhET 项目的改造实践编写。遵循 PhET 的开源许可证。

如有改进建议或问题反馈，请联系 AI Education Project Team。
