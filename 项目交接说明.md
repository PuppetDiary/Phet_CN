# PhET_CN 项目交接说明（简版）

## 1. 项目基本构成

本仓库是一个 PhET 多项目工作区，主要由三部分组成：

1. 模拟器项目目录  
如 `energy-skate-park/`、`forces-and-motion-basics/`、`acid-base-solutions/` 等，每个目录是一个独立模拟器。

2. 公共依赖与框架目录  
如 `chipper/`、`joist/`、`scenery/`、`sun/`、`axon/`、`bamboo/`、`griddle/` 等，用于构建和运行模拟器。

3. 项目文档与脚本  
如流程文档、上线清单、发布脚本（`publish-gitee-project.ps1`）和提示词文件。

## 2. 中文化 HTML 文件位置

当前已构建的中文化 HTML 文件统一放在 `game_v2` 文件夹中。  
后续交付、测试、演示优先从 `game_v2` 目录取文件。

## 3. 后续改造方式（重点）

后续如需继续做“改造指令生成”，不需要重新整理复杂规则，直接将下面文件内容输入大模型即可：

- [固定的修改prompt.txt](d:/Yongji_Wu/projects/Phet/固定的修改prompt.txt)

即：按既有流程，使用该固定 prompt 作为统一指令模板继续推进即可。
