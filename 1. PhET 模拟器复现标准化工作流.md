---
title: PhET 模拟器复现标准化工作流
stage: "第一步：模拟器本地复现"
stage_index: 1
next_stage: "第二步：游戏化引导与反馈系统改造"
next_document: "case/phet-game-tutorial-enhancement-guide.md"
version: "v1.1"
last_updated: "2026-03-04"
projects:
  - forces-and-motion-basics
  - energy-skate-park
---
# PhET 模拟器复现标准化工作流

---

| 字段 | 内容 |
|------|------|
| **版本** | v1.1 |
| **最后更新** | 2026-03-03 |
| **项目阶段** | 🚀 **第一步：模拟器本地复现**（克隆 → 构建 → 验证可运行） |
| **下一步** | 第二步：定制化改造（添加中文界面、自定义功能） |

> [!NOTE]
> 本文档是国科信 AI 教育项目 PhET 模拟器改造工作的**起点文档**。完成本工作流后，即可对模拟器进行源码级定制开发。

---

本文档描述了如何从 GitHub 仓库克隆并构建任何 PhET 交互式模拟器的完整流程（**默认构建中文版本**）。

## 前置条件

### Node.js 版本要求

> [!IMPORTANT]
> PhET 构建工具要求 **Node.js 18.18 或更高版本，或者 Node.js 20.9 或更高版本**（官方校验值）。
> 建议使用 nvm 管理版本：
> ```bash
> # 检查 node 版本
> node --version  # 须 >= 18.18.0
> 
> # 如果低于要求，使用 nvm 切换
> nvm use 25  # 或任意 >= 18.18 的版本
> ```
> 测试使用的 Node.js 版本为 **v25.2.1**，路径为 `~/.nvm/versions/node/v25.2.1/bin/`。
>
> 如果你的 shell 环境无法自动识别 nvm 版本，请使用完整路径：
> ```bash
> export PATH=$HOME/.nvm/versions/node/v25.2.1/bin:$PATH
> ```

### 已完成的准备工作（一次性）

1. **克隆 PhET 框架仓库**
   ```bash
   cd /Users/yangsu/Demo/University/Tsinghua/Project/Zhipu/国科信/AI_Education/phet-dev

   # 克隆21个核心框架仓库
   git clone --branch main --single-branch https://github.com/phetsims/axon.git
   git clone --branch main --single-branch https://github.com/phetsims/babel.git
   git clone --branch main --single-branch https://github.com/phetsims/brand.git
   git clone --branch main --single-branch https://github.com/phetsims/chipper.git
   git clone --branch main --single-branch https://github.com/phetsims/dot.git
   git clone --branch main --single-branch https://github.com/phetsims/joist.git
   git clone --branch main --single-branch https://github.com/phetsims/kite.git
   git clone --branch main --single-branch https://github.com/phetsims/perennial.git
   git clone --branch main --single-branch https://github.com/phetsims/perennial-alias.git
   git clone --branch main --single-branch https://github.com/phetsims/phet-core.git
   git clone --branch main --single-branch https://github.com/phetsims/phetcommon.git
   git clone --branch main --single-branch https://github.com/phetsims/phetmarks.git
   git clone --branch main --single-branch https://github.com/phetsims/query-string-machine.git
   git clone --branch main --single-branch https://github.com/phetsims/scenery.git
   git clone --branch main --single-branch https://github.com/phetsims/scenery-phet.git
   git clone --branch main --single-branch https://github.com/phetsims/sherpa.git
   git clone --branch main --single-branch https://github.com/phetsims/sun.git
   git clone --branch main --single-branch https://github.com/phetsims/tambo.git
   git clone --branch main --single-branch https://github.com/phetsims/tandem.git
   git clone --branch main --single-branch https://github.com/phetsims/utterance-queue.git
   ```

2. **安装框架依赖**
   ```bash
   # 为主要框架安装npm依赖
   cd chipper && npm install && cd ..
   cd perennial && npm install && cd ..
   cd example-sim && npm install && cd ..
   ```

3. **安装常用依赖库**
   ```bash
   # 克隆常用依赖库
   git clone --branch main --single-branch https://github.com/phetsims/bamboo.git
   git clone --branch main --single-branch https://github.com/phetsims/griddle.git
   git clone --branch main --single-branch https://github.com/phetsims/twixt.git

   # 安装依赖
   cd bamboo && npm install && cd ..
   cd griddle && npm install && cd ..
   cd twixt && npm install && cd ..
   ```

## 标准化复现流程

### 步骤 1：克隆目标模拟器仓库

```bash
cd /Users/yangsu/Demo/University/Tsinghua/Project/Zhipu/国科信/AI_Education/phet-dev

# 替换为你要克隆的模拟器名称
git clone --branch main --single-branch https://github.com/phetsims/SIMULATION-NAME.git
```

**示例**：
```bash
git clone --branch main --single-branch https://github.com/phetsims/forces-and-motion-basics.git
```

> [!IMPORTANT]
> **目录命名规则**：克隆后的目录名 **必须与 `package.json` 的 `name` 字段一致**（通常就是 GitHub 仓库名）。
> PhET 构建工具通过 `path.basename(process.cwd())` 获取仓库名，如果目录名与 `package.json` 中的 `name` 不一致，webpack 将无法找到编译输出。
>
> 🚫 **错误做法**：将仓库克隆到 `energy-skate-park-main/`
> ✅ **正确做法**：将仓库克隆到 `energy-skate-park/`（与 `package.json` 的 `name` 字段一致）
>
> 如果目录名不匹配，使用 `mv` 重命名：
> ```bash
> mv energy-skate-park-main energy-skate-park
> ```

### 步骤 2：检查依赖关系

```bash
cd SIMULATION-NAME

# 查看 package.json 中的 phet.phetLibs 字段
cat package.json | grep -A 10 "phetLibs"
```

如果 `phetLibs` 不为空，需要克隆这些依赖：

```bash
# 示例：如果 phetLibs 包含 "circuit-construction-kit-common", "bamboo", "griddle"
cd ..
git clone --branch main --single-branch https://github.com/phetsims/DEPENDENCY-NAME.git
cd DEPENDENCY-NAME && npm install && cd ..
```

### 步骤 3：编译依赖库（如果有 phetLibs）

如果有依赖库（如 bamboo, griddle 等），需要先编译它们：

```bash
# 从依赖库自己的目录编译
cd DEPENDENCY-NAME
npx grunt output-js-project
cd ..
```

**示例**：
```bash
# 编译 bamboo
cd bamboo
npx grunt output-js-project
cd ..

# 编译 circuit-construction-kit-common（如果有）
cd circuit-construction-kit-common
npx grunt output-js-project
cd ..
```

### 步骤 4：安装模拟器的 npm 依赖

```bash
cd SIMULATION-NAME
npm install
```

### 步骤 5：跳过 Lint 检查（推荐）

> [!TIP]
> 大部分情况下需要跳过 Lint，因为框架仓库可能有本地修改。有两种方式：

**方式 A：命令行参数（推荐，当 `build-local.json` 无效时使用）**
```bash
npx grunt --brands=adapted-from-phet --locales=zh_CN --lint=false
```

**方式 B：配置文件**
```bash
echo '{"skipLint": true}' > build-local.json
```

> [!WARNING]
> 如果框架仓库（如 `joist`）有本地自定义修改，仅 `build-local.json` 的 `skipLint` 可能不足以完全跳过 lint，**优先使用 `--lint=false` 命令行参数**。

### 步骤 5.5：更新框架版本（如果有缺失模块）

> [!IMPORTANT]
> **如果模拟器源码使用了框架中尚不存在的 API**（例如新添加的类或方法），需要更新对应框架：

```bash
# 返回 phet-dev 根目录
cd /Users/yangsu/Demo/University/Tsinghua/Project/Zhipu/国科信/AI_Education/phet-dev

# 根据缺失模块所在仓库，git pull 更新对应框架
cd scenery-phet && git pull origin main && cd ..
cd scenery && git pull origin main && cd ..
cd sherpa && git pull origin main && cd ..
cd sun && git pull origin main && cd ..
cd axon && git pull origin main && cd ..
# ..等其他报错的框架

# 更新后重新 cd 到模拟器目录
cd SIMULATION-NAME
```

更新框架后必须**重新运行 transpile**：
```bash
npx grunt output-js-project
```

### 步骤 6：构建模拟器（中文版）

```bash
# 构建中文版（推荐）
npx grunt --brands=adapted-from-phet --locales=zh_CN --lint=false
```

> [!TIP]
> `--locales=zh_CN` 会在构建产物中生成中文界面版本。
> - 如需同时包含英文版，可用 `--locales=en,zh_CN`
> - 如需包含所有语言，省略 `--locales` 参数即可（构建时间更长）

**预期输出**：
```
Running "default" task
Running "clean"
Running "build"
>> Transpile complete:          XXXms
Building runnable repository (SIMULATION-NAME, brands: adapted-from-phet)
>> Webpack build complete:      XXXms
>> Production minify complete:  XXXms
>> Brand adapted-from-phet complete:  XXXms
```

### 步骤 7：验证构建结果

```bash
ls -lah build/adapted-from-phet/
```

**预期文件（构建中文版后）**：
- `SIMULATION-NAME_zh_CN_adapted-from-phet.html` - 单文件**中文版**（可直接在浏览器打开）✅
- `SIMULATION-NAME_en_adapted-from-phet.html` - 单文件英语版本（若 locales 包含 en）
- `SIMULATION-NAME_all_adapted-from-phet.html` - 包含所有语言的版本
- `SIMULATION-NAME_all_adapted-from-phet_debug.html` - 调试版本

### 步骤 8：测试运行

**方式 1：使用开发服务器（推荐用于开发）**
```bash
# 启动开发服务器
npx grunt dev-server

# 在浏览器中访问（注意：使用项目目录名而非 package.json 的 name）
# http://localhost/energy-skate-park/energy-skate-park_en.html
```

**方式 2：直接打开 HTML 文件（用于测试）**
```bash
# macOS
open build/adapted-from-phet/SIMULATION-NAME_zh_CN_adapted-from-phet.html

# Linux
xdg-open build/adapted-from-phet/SIMULATION-NAME_zh_CN_adapted-from-phet.html

# Windows
start build/adapted-from-phet/SIMULATION-NAME_zh_CN_adapted-from-phet.html
```

### 步骤 8.5：进入 HTML 重构前的清理（必做）

> [!IMPORTANT]
> 在进入 HTML 重构前，先完成这两项清理，避免后续验收返工：  
> 1. 在模拟器“关于”面板中，去掉版本号后面的 `dev.num` 字样。  
> 2. 去掉游戏内快捷键。  

## 常见问题排查

### 问题 0：npm/node 版本不符合要求

**错误信息**：
```
Error: Node 18.18 or 20.9 or greater is needed to run PhET build tools
```

**解决方案**：
```bash
# 使用 nvm 切换到符合要求的版本（推荐 LTS 或 v25+）
nvm use 25
# 或者通过 PATH 指定
export PATH=$HOME/.nvm/versions/node/v25.2.1/bin:$PATH
```

### 问题 0.5：构建路径错误（能找到 `energy-skate-park-main`）

**错误信息**：
```
ModuleNotFoundError: Module not found: Error: Can't resolve '../chipper/dist/js/DIRNAME/js/DIRNAME-main.js'
```

**原因**：PhET 构建工具用 `path.basename(process.cwd())` 获取仓库名，**目录名必须与 `package.json` 的 `name` 字段一致**。符号链接（symlink）无效，webpack 会解析真实路径。

**解决方案**：直接重命名目录：
```bash
mv energy-skate-park-main energy-skate-park  # 改为与 package.json name 一致
```

### 问题 1：模块找不到

**错误信息**：
```
Module not found: Error: Can't resolve '../../DEPENDENCY/js/FILE.js'
```

**解决方案**：
1. 检查 `package.json` 中的 `phetLibs` 字段
2. 克隆缺失的依赖仓库
3. 安装依赖：`cd DEPENDENCY && npm install && cd ..`
4. 编译依赖：`cd DEPENDENCY && npx grunt output-js-project && cd ..`

**如果是框架文件缺失（非 phetLibs 中的依赖）**，说明框架版本过旧：
```bash
# 更新对应框架仓库
git -C FRAMEWORK-NAME pull origin main
# 然后重新 transpile
npx grunt output-js-project
```

### 问题 2：品牌不支持

**错误信息**：
```
AssertionError [ERR_ASSERTION]: Unsupported brand: adapted-from-phet
```

**解决方案**：
检查 `package.json` 中的 `supportedBrands` 字段，确保包含 `"adapted-from-phet"`。如果不包含，使用：
```bash
npx grunt --brands=phet
```

### 问题 3：Lint 错误

**错误信息**：
```
Lint failed
```

**解决方案**：
创建 `build-local.json` 文件跳过 lint：
```bash
echo '{"skipLint": true}' > build-local.json
```

### 问题 4：npm install 失败

**错误信息**：
```
npm ERR! code ENOENT
```

**解决方案**：
```bash
# 清理缓存重试
rm -rf node_modules package-lock.json
npm install
```

### 问题 5：构建任务立即退出（Exit code 1/3）无错误输出

**错误现象**：
```
Running "build" task
```
然后进程直接退出，没有进一步输出。

**原因分析**：
1. 某些模拟器有 `phetLibs` 依赖（如 `vibe`, `tappi`, `griddle`）
2. 这些依赖库需要先编译到 `chipper/dist/js/` 目录
3. 使用 `npx grunt` 命令时，内部调用的 `tsx` 模块可能损坏，导致任务静默失败

**解决方案**：

**步骤 1：检查是否有 phetLibs 依赖**
```bash
cd SIMULATION-NAME
cat package.json | grep -A 5 "phetLibs"
```

**步骤 2：编译缺失的依赖库（使用 tsx 直接调用）**
```bash
# 进入依赖库目录，使用 tsx 直接执行 transpile
cd /data/gkq/phet-dev/DEPENDENCY-NAME
node ../perennial-alias/node_modules/.bin/tsx ../chipper/js/grunt/tasks/transpile.ts

# 例如编译 vibe
cd /data/gkq/phet-dev/vibe
node ../perennial-alias/node_modules/.bin/tsx ../chipper/js/grunt/tasks/transpile.ts

# 例如编译 tappi
cd /data/gkq/phet-dev/tappi
node ../perennial-alias/node_modules/.bin/tsx ../chipper/js/grunt/tasks/transpile.ts
```

**步骤 3：使用 tsx 直接执行构建**
```bash
cd /data/gkq/phet-dev/SIMULATION-NAME
node ../perennial-alias/node_modules/.bin/tsx ../chipper/js/grunt/tasks/build.ts --brands=adapted-from-phet --locales=zh_CN
```

> [!TIP]
> 如果 tsx 模块损坏，重新安装：
> ```bash
> cd /data/gkq/phet-dev/perennial-alias
> npm install tsx --save-dev
> ```

### 问题 6：ts 模块找不到（tsx 内部错误）

**错误信息**：
```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/path/to/package-XXXXXX.mjs'
```

**原因**：tsx 模块缓存损坏

**解决方案**：
```bash
cd /data/gkq/phet-dev/perennial-alias
rm -rf node_modules package-lock.json
npm install tsx --save-dev
```

## 成功案例

### 案例 0：energy-skate-park（有框架版本不匹配问题）

```bash
# 1. 克隆（注意：目录名必须是 energy-skate-park，不能是其他名称）
git clone --branch main --single-branch https://github.com/phetsims/energy-skate-park.git
# 如果已有目录名不对，重命名：mv energy-skate-park-main energy-skate-park

# 2. 检查依赖（phetLibs 中有 griddle）
cd energy-skate-park
cat package.json | grep -A5 phetLibs
# 输出："phetLibs": ["griddle"]
# griddle 已在前置步骤中克隆，可直接跳过克隆

# 3. 安装 npm 依赖
npm install

# 4. 更新所有框架（energy-skate-park 依赖较新的 API）
cd ..
git -C scenery-phet pull origin main
git -C scenery pull origin main
git -C sherpa pull origin main
git -C axon pull origin main
git -C sun pull origin main
git -C chipper pull origin main
cd energy-skate-park

# 5. 编译依赖库（griddle 需要 transpile）
npx grunt output-js-project

# 6. 构建（中文版）
echo '{"skipLint": true}' > build-local.json
npx grunt --brands=adapted-from-phet --locales=zh_CN --lint=false

# 7. 测试
open build/adapted-from-phet/energy-skate-park_zh_CN_adapted-from-phet.html
```

### 案例 1：forces-and-motion-basics（无额外依赖）

```bash
# 1. 克隆
git clone --branch main --single-branch https://github.com/phetsims/forces-and-motion-basics.git

# 2. 安装依赖
cd forces-and-motion-basics
npm install

# 3. 构建（中文版）
echo '{"skipLint": true}' > build-local.json
npx grunt --brands=adapted-from-phet --locales=zh_CN --lint=false

# 4. 测试
open build/adapted-from-phet/forces-and-motion-basics_zh_CN_adapted-from-phet.html
```

### 案例 2：circuit-construction-kit-dc（有额外依赖）

```bash
# 1. 克隆主项目
git clone --branch main --single-branch https://github.com/phetsims/circuit-construction-kit-dc.git

# 2. 克隆依赖
git clone --branch main --single-branch https://github.com/phetsims/circuit-construction-kit-common.git
git clone --branch main --single-branch https://github.com/phetsims/bamboo.git
git clone --branch main --single-branch https://github.com/phetsims/griddle.git
git clone --branch main --single-branch https://github.com/phetsims/twixt.git

# 3. 安装依赖
cd bamboo && npm install && cd ..
cd griddle && npm install && cd ..
cd twixt && npm install && cd ..
cd circuit-construction-kit-common && npm install && cd ..
cd circuit-construction-kit-dc && npm install && cd ..

# 4. 编译依赖库
cd bamboo && npx grunt output-js-project && cd ..
cd circuit-construction-kit-common && npx grunt output-js-project && cd ..

# 5. 修复 lint 错误（如果需要）
cd circuit-construction-kit-dc
# 手动修复 js/lab/view/LabScreenView.ts:35 的类型断言问题

# 6. 构建主项目（中文版）
echo '{"skipLint": true}' > build-local.json
npx grunt --brands=adapted-from-phet --locales=zh_CN --lint=false

# 7. 测试
open build/adapted-from-phet/circuit-construction-kit-dc_zh_CN_adapted-from-phet.html
```

## 批量复现脚本

如果要复现多个模拟器，可以创建脚本：

```bash
#!/bin/bash
# reproduce-sims.sh

SIMULATIONS=(
    "forces-and-motion-basics"
    "energy-skate-park"
    "balancing-act"
    # 添加更多模拟器名称
)

cd /Users/yangsu/Demo/University/Tsinghua/Project/Zhipu/国科信/AI_Education/phet-dev

for SIM in "${SIMULATIONS[@]}"; do
    echo "========================================="
    echo "开始复现: $SIM"
    echo "========================================="

    # 克隆
    git clone --branch main --single-branch "https://github.com/phetsims/$SIM.git"

    # 安装依赖
    cd "$SIM"
    npm install

    # 构建（中文版）
    echo '{"skipLint": true}' > build-local.json
    npx grunt --brands=adapted-from-phet --locales=zh_CN --lint=false

    # 验证
    if [ -f "build/adapted-from-phet/${SIM}_zh_CN_adapted-from-phet.html" ]; then
        echo "✓ $SIM 构建成功！"
    else
        echo "✗ $SIM 构建失败！"
    fi

    cd ..
done
```

## 目录结构说明

```
phet-dev/
├── axon/                          # 框架：状态管理
├── bamboo/                        # 依赖库：图表组件
├── chipper/                       # 框架：构建工具链
│   └── dist/js/                   # 编译输出目录
│       ├── bamboo/                # 编译后的 bamboo
│       ├── circuit-construction-kit-common/
│       └── [其他依赖]/
├── circuit-construction-kit-dc/   # 模拟器：直流电路
│   ├── build/adapted-from-phet/   # 构建输出
│   │   ├── circuit-construction-kit-dc_zh_CN_adapted-from-phet.html  # 中文版
│   │   └── circuit-construction-kit-dc_all_adapted-from-phet.html
│   ├── js/                        # TypeScript 源代码
│   └── package.json               # 项目配置
├── forces-and-motion-basics/      # 模拟器：力与运动基础
│   ├── build/adapted-from-phet/
│   │   ├── forces-and-motion-basics_zh_CN_adapted-from-phet.html     # 中文版
│   │   └── forces-and-motion-basics_all_adapted-from-phet.html
│   └── js/                        # TypeScript 源代码
└── [其他框架和模拟器...]
```

## 技术要点

### 1. PhET 框架架构（SceneryStack）

- **Scenery**: HTML5 场景图渲染引擎
- **Joist**: UI 框架，处理模拟器控制界面
- **Axon**: 状态管理和响应式 Property 系统
- **Chipper**: 构建工具链和开发工具
- **Sherpa**: 第三方 JavaScript 库集合

### 2. 构建系统

- **grunt dev-server**: 开发服务器，支持热重载
- **grunt output-js-project**: 将 TypeScript 转译为 JavaScript
- **grunt --brands=adapted-from-phet**: 构建单文件 HTML（用于本地使用）
- **grunt --brands=phet**: 构建 PhET 官方品牌版本

### 3. 品牌类型

- **phet**: PhET 官方品牌（包含 PhET logo 和品牌元素）
- **adapted-from-phet**: 改编版本（去品牌化，适合二次开发）
- **phet-io**: 研究版本（包含数据采集功能）

### 4. 文件类型

- **源代码**: TypeScript 文件（`.ts`）在 `js/` 目录
- **编译输出**: JavaScript 文件在 `chipper/dist/js/` 目录
- **单文件 HTML**: 包含所有依赖的独立 HTML 文件（3-5 MB）

## 下一步：开发新功能

成功复现后，你可以：

1. **研究源代码**
   ```bash
   cd SIMULATION-NAME/js
   # 查看主要的 TypeScript 文件
   ```

2. **修改代码**
   - 编辑 `js/` 目录下的 TypeScript 文件
   - 运行 `npx grunt` 重新构建
   - 刷新浏览器查看更改

3. **创建新屏幕**
   - 参考 `js/screens/` 中的现有屏幕
   - 复制并修改为新的实验场景

4. **本地化**
   - 修改字符串文件：`*strings_en.yaml`
   - 添加中文翻译：`*strings_zh.yaml`

## 参考资源

- **PhET 开发文档**: https://github.com/phetsims/chipper/blob/main/doc/development-guide.md
- **PhET 官网**: https://phet.colorado.edu/
- **所有模拟器列表**: https://github.com/phetsims?tab=repositories&q=sim&type=&language=&sort=

## 总结

这个标准化工作流让你能够：

✅ **快速复现**：只需提供 GitHub 链接，按照步骤操作即可
✅ **离线运行**：生成的 HTML 文件完全离线，无需网络
✅ **二次开发**：基于源代码进行定制和扩展
✅ **批量处理**：使用脚本自动化复现多个模拟器

祝开发顺利！🚀
